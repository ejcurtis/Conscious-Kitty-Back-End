const router = require('express').Router();

router.get('/:phrase/nutrition', async (req, res) => {
  await axios('') => {
    // a bunch of nutrition shit
    res.send({ success: shit })
  })
})

module.exports = router;